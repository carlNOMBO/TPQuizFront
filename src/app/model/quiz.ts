export class Quiz {
    /*id?;
	titre?: string;
	description?: string;
	questions?: {
        id?;
        question?: string;
        isMultiple?: boolean;
        reponses?:{
            id?;
            reponse?: string
            isTrue?: boolean}[]
    }[]*/
}
